[tox]
envlist = d{20,21}-py{36}

[testenv]
extras=test
deps=
     pipenv

commands =
    {posargs:pytest tests}

passenv = PWD

[testenv:d21-py36]
commands =
    pip install django>=2.1a --pre
    {posargs:pytest tests}

