quiet: false
debug: false
format: table
ignore-policy:

cache-dir: .trivy-cache

severity:
  - UNKNOWN
  - LOW
  - MEDIUM
  - HIGH
  - CRITICAL

vulnerability:
#  # Same as '--vuln-type'
#  # Default is 'os,library'
  type:
    - os
    - library
