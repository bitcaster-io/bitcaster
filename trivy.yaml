quiet: true
debug: true
format: table
ignore-policy:

cache-dir: .trivy-cache

fs:
  skip-files:
    - .env
    - .envrc
scanners:
  - pippo

severity:
  - UNKNOWN
  - LOW
  - MEDIUM
  - HIGH
  - CRITICAL

#vulnerability:
##  # Same as '--vuln-type'
##  # Default is 'os,library'
#  type:
#    - os
#    - library
